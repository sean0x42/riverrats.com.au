<% content_for :title, "Edit #{@venue.name} (Admin)" %>

<div class="admin-body">
    <h1>Edit <%= @venue.name %></h1>
    <p class="form-description">Make some changes to this venue.</p>
    <%= render 'form' %>
</div>

<div class="danger-zone admin-body" style="margin-top: 2rem;">

    <h1><i class="material-icons">&#xE002;</i>Danger Zone</h1>
    <p class="form-description">Be careful! These actions could have unforeseen consequences, and cannot be undone.</p>

    <h2>Delete venue</h2>
    <p><strong>Permanently</strong> delete <%= @venue.name %> from the database. You will not be able to recover it.</p>
    <div class="buttons">
        <%= link_to admin_venue_path(@venue), method: :delete, class: 'button danger', data: { confirm: t('venue.confirm_delete')} do %>
            Delete <%= @venue.name %>
        <% end %>
    </div>

</div>