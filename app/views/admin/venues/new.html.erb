<%- content_for :title, 'New Venue (Admin)' %>

<!-- Header -->
<div class="wrapper-header admin">
    <h1 class="has-subtitle">New Venue</h1>
    <p class="subtitle">Here you can create a new venue.</p>
</div>

<div class="column-wrapper small-first">

    <!-- Navigation -->
    <div class="thin">
        <%= render 'admin/navigation' %>
    </div>

    <div class="wide">

        <!-- Form -->
        <div class="background">
            <h2>Form</h2>
            <%= render 'form' %>
        </div>

    </div>

</div>