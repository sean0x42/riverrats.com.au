<%- content_for :title, 'New player (Admin)' %>

<div class="column-wrapper small-first">

  <!-- Navigation -->
  <div class="thin">
    <%= render 'admin/navigation' %>
  </div>

  <div class="wide">

    <!-- Header -->
    <div class="wrapper-header admin">
      <div>
        <h1 class="has-subtitle">Create a player</h1>
        <p class="subtitle">Register a new player. The player will be sent a randomly generated password via
          email.</p>
      </div>
    </div>

    <!-- Form -->
    <div class="background">
      <h2>Form</h2>
      <%= render 'form' %>
    </div>

  </div>

</div>